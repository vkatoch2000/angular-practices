import { UpperCasePipePipe } from './upper-case-pipe.pipe';

describe('UpperCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
